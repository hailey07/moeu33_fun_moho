这里有一个图层脚本，你可以将其嵌入到切换图层中。它会在该图层上显示标记，这样你就可以读取名称，而不必仅仅依赖颜色标签。它还会显示插值，因此如果你在切换图层上启用“插值子图层”并选择使用步进键来控制动作，就可以轻松区分动作和缓动。

![](https://www.lukaskrepel.nl/temp/moho/LK_SwitchMarkers.png)

![](https://www.lukaskrepel.nl/temp/moho/LK_SwitchMarkers_NegativeCycle.gif)